Caches
======

This directory is for caches. Everything here should be safe to delete unless you're running in prod, in which case make sure to rebuild if you delete something here.

Things cached here:

- `index-old.html`, `index-new.html` Built index files.
