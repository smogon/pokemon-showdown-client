services:
  mysql:
    image: mysql
    environment:
      MYSQL_HOST: localhost
      MYSQL_DATABASE: test
      MYSQL_USER: test
      MYSQL_PASSWORD: test
    ports:
      - 3307
    expose:
      - 3307
